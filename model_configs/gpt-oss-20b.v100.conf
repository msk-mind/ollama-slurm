# GPT-OSS 20B - V100 Profile
# V100: 16GB VRAM, max 4 per node
# Model: ~12GB (MXFP4 native) split across 2 GPUs (6GB/GPU)
# KV cache + buffers at 128K: ~6GB total (3GB/GPU)
# Total per GPU: ~9GB -> fits on V100 16GB
# HF: ggml-org/gpt-oss-20b-GGUF
MODEL_FILE="~/.cache/llama.cpp/gpt-oss-20b-mxfp4.gguf"
CPUS=8
MEM="32G"
GPUS=2
GPU_TYPE="v100"
CONTEXT_SIZE=131072
N_GPU_LAYERS=-1
EXTRA_ARGS="-b 2048 -ub 2048 --temp 1.0 --top-p 1.0 --top-k 0 --min-p 0.0"
