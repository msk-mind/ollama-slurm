# GLM-Z1 32B - A100 Profile
# A100: 80GB VRAM, max 4 per node
# Standard GLM4 dense transformer (not MoE; no DeepSeek2 MLA overhead)
# Model: ~19GB (Q4_K_M), KV cache at 128K: ~26GB
# Total VRAM: ~45GB -> fits on 1x A100 with headroom
# GLM-Z1 is a reasoning model: emits <think>...</think> traces by default.
MODEL_FILE="~/.cache/llama.cpp/GLM-Z1-32B-0414-Q4_K_M.gguf"
CPUS=8
MEM="32G"
GPUS=1
GPU_TYPE="a100"
CONTEXT_SIZE=131072
N_GPU_LAYERS=-1
EXTRA_ARGS=""
